package day1;

public class Books {

    //State

    // size (int);
    // color (String);
    // author (String);
    // isbn   (String);
    // coverPage  (String);
    // price (float);
    // type  (String);

    //Behaviour

    // buy book;
    // sell book;
    // filter by type or price;
    // notify when available;
    // pre-order a book;
    // read a sample


}
